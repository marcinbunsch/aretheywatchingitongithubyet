!!!
%html
  %head
    %title== Are people watching it yet?
    %link{ :rel => 'stylesheet', :href => "styles.css", :type => 'text/css' }

  %body
    %img.octocat{ :src => 'http://octodex.github.com/images/original.jpg' }
    .results
      - if @found.size > 0
        .result
          = @found.size > 1 ? "Do" : "Does"
          %span.usernames
            = @found.join(', ')
          = @repo ? "watch" : "follow"
          %span.watchable
            %a{ :href => "https://github.com/#{@what}" }= @what
          on Github yet?
          %span.big.green
            YES!

      - if @missing.size > 0
        .result
          = @missing.size > 1 ? "Do" : "Does"
          %span.usernames
            = @missing.join(', ')
          = @repo ? "watch" : "follow"
          %span.watchable
            %a{ :href => "https://github.com/#{@what}" }= @what
          on Github yet?
          %span.big.red
            NO!